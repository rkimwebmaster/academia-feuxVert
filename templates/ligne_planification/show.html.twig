{% extends 'base.html.twig' %}

{% block title %}LignePlanification{% endblock %}

{% block body %}
    <h1>LignePlanification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_planification.id }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                <td>{{ ligne_planification.heureDebut ? ligne_planification.heureDebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>HeureFin</th>
                <td>{{ ligne_planification.heureFin ? ligne_planification.heureFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ ligne_planification.observation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ligne_planification_index') }}">back to list</a>

    <a href="{{ path('ligne_planification_edit', {'id': ligne_planification.id}) }}"class="btn btn-default btn-sm"><span class="fa fa-edit"></span></a>

    {{ include('ligne_planification/_delete_form.html.twig') }}
{% endblock %}
