{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-default ">
    <span class="fa fa-download"></span>
    {{ button_label|default('Enregister') }}</button>
{{ form_end(form) }}
