{% extends 'base.html.twig' %}

{% block title %}AnneeAcademique{% endblock %}
{% block lesboutons %}
							<a title="Retour à la liste " class="btn btn-info">
								<i class="fa fa-reply-all"></i>
							</a>
							<a type="button" title="Ajouter nouveau " class="btn btn-info">
								<i class="fas fa-plus-circle"></i>
							</a>
							<a type="button" title="Modifier élément  " class="btn btn-info">
								<i class="fas fa-edit"></i>
							</a>
							<a type="button" title="Supprimer élément" class="btn btn-info">
								<i class="fas fa-trash"></i>
							</a>
						{% endblock %}
                        {% block titre %}
													Dé
												{% endblock %}
{% block body %}
    <h1>AnneeAcademique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annee_academique.id }}</td>
            </tr>
            <tr>
                <th>Debut</th>
                <td>{{ annee_academique.debut }}</td>
            </tr>
            <tr>
                <th>Fin</th>
                <td>{{ annee_academique.fin }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ annee_academique.createdAt ? annee_academique.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ annee_academique.updatedAt ? annee_academique.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('annee_academique_index') }}">back to list</a>

    <a href="{{ path('annee_academique_edit', {'id': annee_academique.id}) }}"class="btn btn-default btn-sm"><span class="fa fa-edit"></span></a>

    {{ include('annee_academique/_delete_form.html.twig') }}
{% endblock %}
