<form method="post" action="{{ path('paiement_depot_delete', {'id': paiement_depot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paiement_depot.id) }}">
    <button class="btn">Delete</button>
</form>
