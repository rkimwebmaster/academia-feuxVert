<form method="post" action="{{ path('broadcast_message_delete', {'id': broadcast_message.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ broadcast_message.id) }}">
    <button class="btn">Delete</button>
</form>
